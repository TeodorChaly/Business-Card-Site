def calculator_res(num_one, num_two, operation):
    if operation == "+":
        return num_one+num_two
    if operation == "-":
        return num_one-num_two
    if operation == "*":
        return num_one*num_two
    if operation == "/":
        return num_one/num_two
